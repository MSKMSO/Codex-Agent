{
  "vm": "openclaw-vm",
  "rg": "SDNeurosurgery-OpenClaw",
  "exit_code": 0,
  "stdout": "reply_chunked(chat_id, reply)\n121:                post_reply(chat_id, reply)\n157:    \"   Common target SKUs: Standard_D4ds_v4 (4c/16G temp-disk), Standard_D4as_v4 (4c/16G AMD), Standard_D8s_v3 (8c/32G), Standard_E8s_v3 (8c/64G mem-heavy). If unsure, ask.\\n\\n\"\n167:    \"G) 'what did X say', 'check earlier in chat', 'look back at last week', or you need conversation context BEYOND what's in this prompt:\\n\"\n169:    \"   <chat-id> = conversation.id from the current inbound. For a different chat, run mskai-search-chats.sh first to find it.\\n\"\n241:    conv = body.get(\"conversation\",{}) or {}\n249:    if conv.get(\"conversationType\") == \"personal\": return True\n264:def local_chat_history(chat_id, current_ts, limit=15):\n274:                    conv = (b.get(\"conversation\") or {}).get(\"id\", \"\")\n275:                    if conv != chat_id:\n295:                    if o.get(\"chat_id\") != chat_id: continue\n349:def _fetch_chat_attachments(chat_id, activity_ts, sender_aad_id):\n356:    if not tok or not chat_id:\n358:    enc = urllib.parse.quote(chat_id, safe=\"\")\n407:def fetch_and_download_attachments(chat_id, activity_ts, sender_aad_id, work_root=\"/tmp/mskai-work\"):\n411:    atts = _fetch_chat_attachments(chat_id, activity_ts, sender_aad_id)\n417:    stem = re.sub(r\"[^A-Za-z0-9_]\", \"_\", (chat_id or \"chat\"))[-40:] + \"-\" + str(int(time.time()))\n429:def build_prompt(body, context, priv, chat_id=None, current_ts=None, attached_files=None):\n437:    if bad_ctx and chat_id:\n438:        context = local_chat_history(chat_id, current_ts, limit=15)\n587:def _translate_chat_id_for_graph_h(conversation_id, sender_aad_id):\n591:    if not conversation_id: return None\n592:    if conversation_id.startswith(\"19:\") and \"@thread.v2\" in conversation_id:\n593:        return conversation_id\n594:    if conversation_id.startswith(\"19:\") and \"@unq.gbl.spaces\" in conversation_id:\n595:        return conversation_id\n596:    if conversation_id.startswith(\"a:\") and sender_aad_id:\n598:    return conversation_id  # last resort, may still 404\n603:def _fetch_history_image_urls(graph_chat_id, max_lookback=HISTORY_IMG_MAX_LOOKBACK):\n609:    if not tok or not graph_chat_id:\n611:    enc = urllib.parse.quote(graph_chat_id, safe=\"\")\n694:            conv = (body.get(\"conversation\") or {}).get(\"id\", \"\") or \"\"\n696:            graph_chat_id = _translate_chat_id_for_graph_h(conv, sender_aad)\n697:            history_urls = _fetch_history_image_urls(graph_chat_id) if graph_chat_id else []\n765:def _post_reply_orig_protect(chat_id, text):\n766:    subprocess.run([SENDER, text, chat_id], timeout=60, capture_output=True, text=True)\n772:                \"chat_id\": chat_id,\n778:def post_reply(chat_id, text):  # BOT_PROTECTIONS_MSKAI_V1 — redact known passwords first\n779:    return _post_reply_orig_protect(chat_id, _redact_passwords_protect(text))\n802:def post_reply_chunked(chat_id, text):\n807:        post_reply(chat_id, body)\n816:    chat_id = (body.get(\"conversation\") or {}).get(\"id\",\"\")\n820:        if _maybe_handle_creds_protect(_ut, chat_id, idx):\n824:    if not chat_id: return\n830:        work_dir, attached = fetch_and_download_attachments(chat_id, env.get(\"ts\"), sender_aad)\n834:                          chat_id=chat_id, current_ts=env.get(\"ts\"),\n858:                if 'post_reply_chunked' in globals(): post_reply_chunked(chat_id, '⚠️ I tried to run a command that looked destructive and refused.')\n859:                else: post_reply(chat_id, '⚠️ I tried to run a command that looked destructive and refused.')\n870:        try: post_reply(chat_id, \"⚠️ *Had trouble generating a reply — please retry in a moment.*\")\n873:    try: post_reply_chunked(chat_id, reply)\n880:# PER_CHAT_PARALLEL_V1 — per-chat-id worker pool. Each chat_id gets a dedicated\n886:def _chat_worker(chat_id, q):\n897:                    log(ERR_LOG, f\"worker[{chat_id[:18]}] L{idx}: {e}\")\n904:                log(ERR_LOG, f\"worker[{chat_id[:18]}] outer: {e}\")\n912:        cid = (body.get(\"conversation\") or {}).get(\"id\", \"\") or \"_default\"\n919:            t = threading.Thread(target=_chat_worker, args=(cid, q),\n=== END ===",
  "stderr": ""
}
