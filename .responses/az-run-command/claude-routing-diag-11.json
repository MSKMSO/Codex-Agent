{
  "vm": "openclaw-vm",
  "rg": "SDNeurosurgery-OpenClaw",
  "exit_code": 0,
  "stdout": "{_danger[:3]}\")\n            try:\n                if 'post_reply_chunked' in globals(): post_reply_chunked(chat_id, '⚠️ I tried to run a command that looked destructive and refused.')\n                else: post_reply(chat_id, '⚠️ I tried to run a command that looked destructive and refused.')\n            except Exception: pass\n            return\n    if not reply:\n        log(ERR_LOG, f\"L{idx} first attempt failed; retry with trimmed prompt + 180s timeout\")\n        trimmed = prompt\n        if len(trimmed) > 8000:\n            trimmed = trimmed[:2500] + \"\\n[... earlier context omitted ...]\\n\" + trimmed[-5500:]\n        reply = run_codex(trimmed, priv, timeout_s=180, extra_dirs=extra_dirs, image_paths=_img_paths)\n    if not reply:\n        log(ERR_LOG, f\"L{idx} empty reply (retry also failed)\")\n        try: post_reply(chat_id, \"⚠️ *Had trouble generating a reply — please retry in a moment.*\")\n        except: pass\n        return\n    try: post_reply_chunked(chat_id, reply)\n=== mskai-responder.py lines 800-870 (handle_activity caller) ===\n        \"--append-system-prompt\", sys_prompt,\n    ]\n    if priv:\n        args += [\"--add-dir\", \"/home/azureuser/.mskai-bot\", \"--add-dir\", \"/tmp/mskai-work\"]\n    for d in (extra_dirs or []):\n        args += [\"--add-dir\", d]\n    # Send the prompt via stdin to avoid --add-dir consuming it as another dir\n    try:\n        r = subprocess.run(args, capture_output=True, text=True, timeout=timeout_s, input=body)\n    except subprocess.TimeoutExpired:\n        log(ERR_LOG, \"claude timeout\"); return None\n    except Exception as e:\n        log(ERR_LOG, f\"claude spawn: {e}\"); return None\n    with open(DEBUG_LOG, \"a\") as f:\n        f.write(\"===== \" + time.strftime(\"%Y-%m-%dT%H:%M:%S\") + \" priv=\" + str(priv) + \" (claude) =====\\n\")\n        f.write(\"RC:\" + str(r.returncode) + \"\\nSTDOUT:\\n\" + (r.stdout or \"\")[:8000] + \"\\n\\n\")\n    # claude -p prints the reply directly — no parsing needed\n    out = (r.stdout or \"\").strip()\n    if r.returncode != 0:\n        log(ERR_LOG, f\"claude rc={r.returncode}: {out[:200]}\")\n        return None\n    low = out.lower()[:300]\n    if \"hit your limit\" in low or \"usage limit\" in low:\n        log(ERR_LOG, f\"claude rate-limited: {out[:200]}\")\n        return None\n    return out or None\n\ndef _post_reply_orig_protect(chat_id, text):\n    subprocess.run([SENDER, text, chat_id], timeout=60, capture_output=True, text=True)\n    # Log every outbound so local_chat_history can show the bot its own past replies\n    try:\n        with open(\"/home/azureuser/.mskai-bot/outbound.jsonl\", \"a\") as _f:\n            _f.write(json.dumps({\n                \"ts\": time.strftime(\"%Y-%m-%dT%H:%M:%S\"),\n                \"chat_id\": chat_id,\n                \"text\": (text or \"\")[:2000],\n            }) + \"\\n\")\n    except Exception: pass\n\n\ndef post_reply(chat_id, text):  # BOT_PROTECTIONS_MSKAI_V1 — redact known passwords first\n    return _post_reply_orig_protect(chat_id, _redact_passwords_protect(text))\n# MSKAI_NOTRUNC_PATCH_V1 - split long replies into safe Teams chunks\ndef _split_for_teams(text, max_chunk=24000):\n    if len(text) <= max_chunk:\n        return [text]\n    chunks = []\n    remaining = text\n    NL2 = chr(10) + chr(10)\n    NL = chr(10)\n    while len(remaining) > max_chunk:\n        split = remaining.rfind(NL2, 0, max_chunk)\n        if split < max_chunk // 2:\n            split = remaining.rfind(NL, 0, max_chunk)\n        if split < max_chunk // 2:\n            split = remaining.rfind('. ', 0, max_chunk)\n        if split < max_chunk // 2:\n            split = max_chunk\n        chunks.append(remaining[:split].rstrip())\n        remaining = remaining[split:].lstrip()\n    if remaining:\n        chunks.append(remaining)\n    return chunks\n\ndef post_reply_chunked(chat_id, text):\n    chunks = _split_for_teams(text)\n    n = len(chunks)\n    for i, c in enumerate(chunks, start=1):\n        body = c if n == 1 else '**(part ' + str(i) + '/' + str(n) + ')**' + chr(10) + chr(10) + c\n        post_reply(chat_id, body)\n        if i < n:\n=== mskai-responder.py grep handle_activity definitions ===\n988:def main():\n=== END ===",
  "stderr": ""
}
