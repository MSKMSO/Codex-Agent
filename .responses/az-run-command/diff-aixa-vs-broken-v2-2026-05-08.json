{
  "vm": "openclaw-vm",
  "rg": "SDNeurosurgery-OpenClaw",
  "exit_code": 0,
  "stdout": ", text=True)\n    try:\n        with open(OUTBOUND, \"a\") as f:\n            f.write(json.dumps({\"ts\": time.strftime(\"%Y-%m-%dT%H:%M:%S\"),\n                \"chat_id\": chat_id, \"text\": (text or \"\")[:2000]}) + \"\\n\")\n    except Exception: pass\n\n\n\ndef post_reply(chat_id, text):  # BOT_PROTECTIONS_AIXA_V1 — redact known passwords first\n    return _post_reply_orig_protect(chat_id, _redact_passwords_protect(text))\n\n\ndef process_line(line, idx):\n    try:\n        env = json.loads(line); body = json.loads(env.get(\"body\", \"{}\"))\n    except Exception as e:\n        log(ERR_LOG, f\"L{idx} parse: {e}\"); return\n    if not is_actionable(body): return\n    chat_id = (body.get(\"conversation\") or {}).get(\"id\", \"\")\n    # BOT_PROTECTIONS_AIXA_V1 — intercept credentials before LLM call.\n    try:\n        _ut = strip_html(body.get(\"text\",\"\") or \"\") if \"strip_html\" in globals() else (body.get(\"text\",\"\") or \"\")\n        if _maybe_handle_creds_protect(_ut, chat_id, idx):\n            return\n    except Exception as _e:\n        log(ERR_LOG, f\"creds-mw outer: {_e}\")\n    if not chat_id: return\n    try: post_reply(chat_id, \"🤔 *On it…*\")\n    except Exception as e: log(ERR_LOG, f\"L{idx} ack post: {e}\")\n    prompt = build_prompt(body, env.get(\"context\", []) or [])\n    reply = run_claude(prompt)\n    if not reply:\n        trimmed = prompt\n        if len(trimmed) > 8000:\n            trimmed = trimmed[:2500] + \"\\n[... earlier context omitted ...]\\n\" + trimmed[-5500:]\n        reply = run_claude(trimmed, timeout_s=180)\n    if not reply:\n        try: post_reply(chat_id, \"⚠️ *Had trouble generating a reply — please retry in a moment.*\")\n        except Exception: pass\n        return\n    if len(reply) > 7000: reply = reply[:6900] + \"\\n\\n(…truncated)\"\n    try: post_reply(chat_id, reply)\n    except Exception as e: log(ERR_LOG, f\"post: {e}\")\n\n\ndef main():\n    log(DEBUG_LOG, \"aixa-responder started\")\n    pool = ThreadPoolExecutor(max_workers=2)\n\n===== Emily post_reply chain (lines 360-410) =====\n\ndef _post_reply_orig_protect(chat_id, text):\n    subprocess.run([SENDER, text, chat_id], timeout=60, capture_output=True, text=True)\n    try:\n        with open(OUTBOUND, \"a\") as f:\n            f.write(json.dumps({\"ts\": time.strftime(\"%Y-%m-%dT%H:%M:%S\"),\n                \"chat_id\": chat_id, \"text\": (text or \"\")[:2000]}) + \"\\n\")\n    except Exception: pass\n\n\n\ndef post_reply(chat_id, text):  # BOT_PROTECTIONS_EMILY_CLAUDE_V1 — redact known passwords first\n    return _post_reply_orig_protect(chat_id, _redact_passwords_protect(text))\ndef _post_reply_orig_protect(chat_id, text):\n    return _post_reply_orig_protect(chat_id, _redact_passwords_protect(text))\n\n\ndef process_line(line, idx):\n    try:\n        env = json.loads(line); body = json.loads(env.get(\"body\", \"{}\"))\n    except Exception as e:\n        log(ERR_LOG, f\"L{idx} parse: {e}\"); return\n    if not is_actionable(body): return\n    chat_id = (body.get(\"conversation\") or {}).get(\"id\", \"\")\n    # BOT_PROTECTIONS_EMILY_CLAUDE_V1 — intercept credentials before LLM call.\n    try:\n        _ut = strip_html(body.get(\"text\",\"\") or \"\") if \"strip_html\" in globals() else (body.get(\"text\",\"\") or \"\")\n        if _maybe_handle_creds_protect(_ut, chat_id, idx):\n            return\n    except Exception as _e:\n        log(ERR_LOG, f\"creds-mw outer: {_e}\")\n    if not chat_id: return\n    try: post_reply(chat_id, \"🤔 *On it…*\")\n    except Exception as e: log(ERR_LOG, f\"L{idx} ack post: {e}\")\n    prompt = build_prompt(body, env.get(\"context\", []) or [])\n    reply = run_claude(prompt)\n    if not reply:\n        trimmed = prompt\n        if len(trimmed) > 8000:\n            trimmed = trimmed[:2500] + \"\\n[... earlier context omitted ...]\\n\" + trimmed[-5500:]\n        reply = run_claude(trimmed, timeout_s=180)\n    if not reply:\n        try: post_reply(chat_id, \"⚠️ *Had trouble generating a reply — please retry in a moment.*\")\n        except Exception: pass\n        return\n    if len(reply) > 7000: reply = reply[:6900] + \"\\n\\n(…truncated)\"\n    try: post_reply(chat_id, reply)\n    except Exception as e: log(ERR_LOG, f\"post: {e}\")\n\n\ndef main():",
  "stderr": ""
}
